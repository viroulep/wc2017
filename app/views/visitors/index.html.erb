<!-- Page Content -->
<div class="container">

    <hr>

    <!-- Call to Action Well -->
    <div class="row">
        <div class="col-lg-12">
            <div class="well text-center">
                <%= t('homepage.welcome_html', login_link: link_to(t('homepage.login'), signin_path)) %>
                <% if params[:with_scopes] %>
                    <br/>
                    <%= link_to "With scopes", signin_path(admin: true) %>
                    <br/>
                <% end %>
            </div>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->

    <!-- Content Row -->
    <div class="row">
        <div class="col-md-6">
            <h2><%= t('homepage.how_to.title') %></h2>
            <p><%= t('homepage.how_to.content') %></p>
            <h2><%= t('homepage.contact.title') %></h2>
            <p><%= t('homepage.contact.content') %> <a href='mailto:onlycube69@gmail.com'>onlycube69@gmail.com</a>.</p>
        </div>
        <div class="col-md-6">
            <%= image_tag('affiche.jpg') %>
        </div>
    </div>
    <!-- /.row -->

    <!-- Footer -->
    <footer>
        <div class="row">
            <div class="col-lg-12">
                <p></p>
            </div>
        </div>
    </footer>

</div>
<!-- /.container -->

